syntax = "proto3";
package demo;

option go_package = "github.com/cloud-fitter/gen/demo";

import "google/api/annotations.proto";

message StringMessage {
  string value = 1;
}

service YourService {
  rpc Echo(StringMessage) returns (StringMessage) {
    option (google.api.http) = {
      post: "/v1/demo"
      body: "*"
    };
  }
}